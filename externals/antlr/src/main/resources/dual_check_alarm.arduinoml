application dual_checker

# Declaring bricks
sensor button1: 8
sensor button2: 9
actuator led: 12

# Declaring states

-> off {
    led <= LOW
    button1 is HIGH and button2 is HIGH => on
}

on {
    led <= HIGH
    button1 is LOW => off
    button2 is LOW => off
}
